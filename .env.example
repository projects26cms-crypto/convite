# Copia este archivo a .env.local y rellena los valores desde
# Supabase → Settings → API.

NEXT_PUBLIC_SUPABASE_URL=

# NUNCA con prefijo NEXT_PUBLIC_. Esta clave salta RLS: solo servidor.
SUPABASE_SERVICE_ROLE_KEY=
